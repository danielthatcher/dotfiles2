#! /bin/bash

OPTS=("full screen" "selection")
case "$( printf '%s\n' "${OPTS[@]}" | rofi -dmenu )" in
    "full screen")
        scrot -e 'mv $f ~/Documents/screenshots/'
        ;;
    "selection")
        scrot -s -e 'mv $f ~/Documents/screenshots/'
        ;;
esac
